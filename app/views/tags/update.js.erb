// Close the edit dialog
if ($('div#editDialog').dialog('isOpen')) {$('div#editDialog').empty().dialog('close');}
// tag has changed, rerender tag's row
var new_row = "<%=escape_javascript( render(:partial => 'tag', :locals =>	{ tag: @tag})) %>";
Ecoport.updateRow("#objId_<%=@tag.id%>", new_row);
