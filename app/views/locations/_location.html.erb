<tr>
	<td>
  	<%= render :partial => "shared/action",
  	 :locals =>	{
  	 	 	:action => 'edit',
  		 	:id => location.id, 
  			:label => 'Edit', 
  			:title => 'Edit Location',
  			:fmt => 'js'}
		 %>
	</td>  <td><%= link_to image_tag("delete-item.png"), { :action => "destroy", :id => location.id },
          :data => { :confirm => 'Are you sure?' }, :method => :delete, :class=> :delete %> </td>
          
  <% Location::INDEX_FIELDS.each do |i| %>
  	<%= content_tag :td, location.send(i) %>
  <% end%>
</tr>