<div id="messages"></div>

<div class="index_page">
	<%= content_tag :h1, "Ecohydrology News Sources", :class => 'ui-widget-header ui-corner-all'%>
  <div  id="actions" class="clearfix">
  	<%= render :partial => "shared/action", :locals => 
  		{ :action => 'new', 
  			:label => 'New', 
  			:title => 'Create News Source',
  			:fmt => 'js'}
		 %>
  </div>

	<table id="news_sources" class="data_table">
		<thead>
			<tr>
				<th>Edit</th>
				<th>Delete</th>
				<% NewsSource::INDEX_FIELDS.each do |i| %>
					<%= content_tag :th, i %>
				<% end %>
			</tr>
		</thead>
		<tbody>
			<%= render :partial => "news_source", :collection => @news_sources%>
		</tbody>
	</table>
</div>
