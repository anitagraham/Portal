<tr id = "objId_<%=news_source.id%>">
	<td>
  	<%= render :partial => "shared/action",
  	 :locals =>	{
  	 	 	:action => 'edit',
  		 	:id => news_source.id, 
  			:label => 'Edit', 
  			:title => 'Edit News Source',
  			:fmt => 'js'}
		 %>
	</td>  <td><%= link_to image_tag("delete-item.png"), { :action => "destroy", :id => news_source.id },
          :data => { :confirm => 'Are you sure?' }, :method => :delete, :class=> :delete %> </td>
          
  <% NewsSource::INDEX_FIELDS.each do |i| %>
  	<%= content_tag :td, news_source.send(i) %>
  <% end%>
</tr>